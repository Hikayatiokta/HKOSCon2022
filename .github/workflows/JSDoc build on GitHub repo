# This is basic workflow to show the manual triger
name : JSDoc build on GitHub repo

on:
  workflow_dispatch

jobs:

  build:
    runs-on: ubuntu-latest 

    steps: 
    - uses: actions/checkout@v

    - name: Say something
      run: echo "Hi, I have been manually triggered!"
